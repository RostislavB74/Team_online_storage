name: Send HEAD request to Server

on:
  schedule:
    - cron: "*/5 5-20 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send request to Server
        run: curl -s -I https://team-online-storage.onrender.com/api/v1/livez/?format=json&git=1444 -o /dev/null